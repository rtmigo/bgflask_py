# I want to check, that the server.py inside BackgroundProcess can import files
# as tests.import_me. Without rewriting PYTHONPATH is it will not work (and
# will fail in GitHub Actions)

FROM_IMPORT_ME = 1
